
## 2026-06-08 01:27 UTC — pre-compaction flush

- GitHub/OPSEC: Lucas definiu acesso por fine-grained PAT com menor privilégio: repositórios do token limitados ao grupo ativo (`tars-openclaw`, `cve-feed`, `projects`, `burguerboxd`), permissões `Contents read/write` e `Metadata read`, sem `Administration`, expiração curta de 90 dias. Regra dura: token nunca vai para chat, markdown, workspace, arquivo versionado, script, log ou histórico; usar apenas dentro do `gh`/credential store e nunca rodar `gh auth token`.
- GitHub workspace: autenticação persistente do `gh` ficou configurada na VM para `github.com` como `andradelucascq`, usando `/home/ubuntu/.config/gh/hosts.yml`. Foi executado `gh auth setup-git`; remote do workspace configurado como `origin https://github.com/andradelucascq/tars-openclaw.git`; branch local `main` teve snapshot inicial commitado e pushado para `origin/main`.
- Snapshot inicial do repo `tars-openclaw`: commit `1df2923` (`chore: initial workspace snapshot 2026-06-08`), autor `Tars <tars@openclaw.local>`, trailer `Co-authored-by: @andradelucascq <51235741+andradelucascq@users.noreply.github.com>`, `31 files changed, 2193 insertions`. Status final relatado: `## main...origin/main`, sem alterações pendentes.
- Hooks OpenClaw analisados, sem alterações: `command-logger` recomendado manter para auditoria; `session-memory` recomendado manter por enquanto como captura bruta em `memory/YYYY-MM-DD-HHMM.md` em `/new`/`/reset`, sem editar `MEMORY.md`; `bootstrap-extra-files` confirmado como leitor/injetor de contexto quando configurado, sem recriar/scaffoldar arquivos; `compaction-notifier` recomendado manter para avisos de compactação.
- ClawHub: CLI já existia em `/home/ubuntu/.openclaw/tools/node/npm/bin/clawhub` versão `0.20.0`; criado symlink `/home/ubuntu/.local/bin/clawhub -> /home/ubuntu/.openclaw/tools/node/npm/bin/clawhub`; skill bundled `clawhub` habilitada em `/home/ubuntu/.openclaw/openclaw.json` (`enabled: false -> true`). Verificado como `✓ Ready`; busca/listagem funcionaram; nenhuma skill externa foi instalada. `clawhub whoami` retornou não logado; recomendação registrada: não logar no ClawHub sem necessidade.
- Governança de skills combinada para formalizar depois: Tars pode buscar/verificar/recomendar skills, mas instalação sempre pergunta antes. Antes de instalar qualquer skill: rodar/verificar com `openclaw skills verify <skill>` e mostrar resultado completo (`skillSpector`, static scan, VirusTotal, assinatura). Resultado `unsigned`, `suspicious` ou `fail` não instala sem ok explícito de Lucas.
- Próximo ponto pendente no fluxo era a Parte D: leitura/verificação de Whisper/Groq/backend de transcrição, mantendo a mesma postura conservadora de não mexer em config sem aprovação explícita.

## 2026-06-08 01:57 UTC — ccusage aplicado como substituto Linux/ARM64

- Lucas aprovou aplicar a recomendação de substituir o `model-usage` macOS/CodexBar-only por `ccusage`.
- Criada e aplicada via Skill Workshop a skill local `usage-report`, usando `npx -y ccusage@20.0.6`, timezone padrão `America/Sao_Paulo`, fontes OpenClaw/Codex/Claude e períodos daily/monthly/session; weekly direto só para Claude/all, enquanto OpenClaw/Codex weekly deve ser resumido via daily com range.
- `ccusage@20.0.6` foi testado: `--version` retornou `ccusage 20.0.6`; `openclaw daily` em JSON para 2026-06-07 America/Sao_Paulo retornou dados locais sem erro.
- `TOOLS.md` atualizado para registrar `usage-report` como ferramenta real do ambiente.

## 2026-06-08 04:29 UTC — Fase 5 encerrada; reflexão self manual

- Lucas deu a Fase 5 como encerrada após instalar a skill `self`, criar `anchor.md` e `SELF.md`, e gravar o `AGENTS.md` em PT.
- Decisão sobre a reflexão da `self`: manter sob gatilho manual por enquanto. Não adicionar cadência automática ao `HEARTBEAT.md` agora.
- Racional do Lucas: o Tars acabou de nascer e ainda não tem histórico real para observar; reflexão a cada 3h sobre interação rasa tenderia a gerar preenchimento artificial. Quando houver uma ou algumas semanas de rodagem, reavaliar ativação conservadora, provavelmente só meso/macro.
- Próximo marco do setup: encerramento do hatching.

## 2026-06-08 05:06 UTC — hatching encerrado; postura reativa por padrão

- Auditoria de leitura (primeira tarefa pós-nascimento) achou descompasso entre intenção e estado real: o OpenClaw heartbeat estava ATIVO por default e já tinha disparado turnos de modelo (ex.: `04:08:55 [heartbeat] started` nos logs do gateway), apesar de a postura pretendida ser reativa. Não havia cron job OpenClaw (`openclaw.cron status`: scheduler on, `jobs: 0`; `list includeDisabled` vazio) nem crontab/timer systemd próprio — o gatilho automático vinha só do heartbeat do gateway. Parei sem mexer e mostrei antes de agir.
- Decisão do Lucas: desligar o disparo automático. Override local `agents.defaults.heartbeat.every = "0m"` gravado em `/home/ubuntu/.openclaw/openclaw.json` (fora do repo, contém token/API key — nunca vai pro git). `HEARTBEAT.md` preservado intacto: a regra dos 2 check-ins de estudo fica escrita como INTENÇÃO, não como automação ativa.
- Confirmado desligado em dois momentos independentes: (1) hot reload antes do restart — `05:01:47 [reload] config change detected → [heartbeat] disabled → hot reload applied`; (2) boot limpo após restart do `openclaw-gateway.service` (PID novo 175167) — `05:02:41 [heartbeat] disabled`. Nenhum turno de heartbeat disparado depois. Config relida: `{ "every": "0m" }`.
- Marco — estado nascido: hatching encerrado. `BOOTSTRAP.md` deletado, seção "Primeira Execução" removida do `AGENTS.md`, commit final `24840ea` (`feat: encerrar hatching - estado nascido`) pushado para `origin/main`.
- Estado operacional atual: REATIVO por padrão — sem cron job, heartbeat off, reflexão da `self` sob gatilho manual. Nada dispara sozinho até religar com calibragem.
- Em aberto para calibrar com rodagem real (não agora): frequência dos check-ins de estudo, se/quando religar o heartbeat, se/quando ativar a reflexão automática da `self`.
